# 更新日志

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [2.8.0] - 2026-09-25

### 新增
- **模型级打印标记**：`files` 表新增 `printed` 列（schema v4 增量迁移，仅加列加索引、旧记录默认未打印，不动既有数据），卡片与详情抽屉可一键标记「已打印/未打印」
- **`POST /api/set-printed`**：`{id, printed}` 更新标记，未知 id 返回 404
- **`GET /api/files` 支持 `printed` 过滤**：`printed=1` 只看已打印、`printed=0` 只看未打印，可与 `q`/`cat`/`status` 等组合
- **前端**：模型库筛选栏新增「打印状态」下拉；卡片新增 🖨 打印徽章（点击切换，已打印绿色/未打印灰色）；详情抽屉「文件信息」新增打印状态行；标记后卡片、待整理页、抽屉同步刷新，开着筛选时自动重查保持总数与分页正确
- 重复上传/合并入库重写同路径记录时保留既有打印标记（`INSERT OR REPLACE` 防护）；组内「打印清单」（`group_members.printed`）与文件级标记各自独立、互不干扰
- **仪表盘「最近打开」**：`files` 表新增 `last_viewed_at` 列（schema v5 增量迁移，仅加列加索引、不动既有数据），打开模型详情抽屉时自动记录打开时间；仪表盘「资产总览」下方新增「最近打开」面板，按打开时间倒序展示最近看过的模型（缩略图 + 归档名 + 打开时间，默认 8 个），点击卡片直接重新打开详情；停留在仪表盘时打开模型，面板静默刷新
- **`POST /api/track-view`**：`{id}` 上报打开动作，未知 id 返回 404；**`GET /api/recent?limit=`**：按 `last_viewed_at` 倒序返回最近打开的文件（默认 8、上限 50）
- 同路径重复入库重写记录时保留既有 `last_viewed_at`（与打印标记同款 `INSERT OR REPLACE` 防护）

## [2.7.0] - 2026-09-19

### 新增
- **合并导出「各自落板」模式（默认）**：保留每个源的板结构——每块源板成为产物的一块板，板内对象相对位置一毫米不动；产物按 Bambu 的板网格公式（板宽 ×1.2 步距、`compute_colum_count` 列数、行偏移走 y 轴）把每块板平移进自己的虚拟床区（Bambu 加载后按实例包围盒与板矩形相交归属分板，不搬进对应虚拟区会被并进第一块板）；`model_settings.config` 写 `<plate>` 段（板名=源文件名-板名）、`model_settings` 的 `instance_id` 恒为 0（对象内实例下标）、`identify_id` 全局唯一；板预览图（plate/top/pick/no_light png）从源包复制并按输出板号重命名，Content_Types 补 png 声明
- **调色板全集拼接**：产物调色板 = 各源 `filament_colour` 全集顺延拼接（不再压缩到实际用到的料槽），对象料槽加源偏移指向全集；`project_settings` 以首个带配置源的完整 JSON 为基底，按料槽对齐数组按三种形态正确扩容——直排（len=n）扩槽沿用末槽、方阵（n²，冲刷矩阵）逐行扩展再补行、组表（料槽×变体，如 `filament_extruder_variant`/`filament_self_index`）保留全表并为新料槽追加末槽组、`filament_self_index` 重编为新料槽号且保持源的类型
- **`GET /api/merge-plates?ids=`**：列出待合并源的板结构（板号/板名/对象数，只读），供选板 UI 使用
- **确认弹窗选板**：合并确认弹窗按源列出各板的板号/板名/对象数（chip 点选，默认全选，可去掉不需要的板；无板结构的源显示「整包 1 板」），提交时把选中板随 `plates` 参数下发；文案同步改为「各自落板」语义
- `/api/merge-export` 支持 `mode`（`plates` 默认 / `single` 旧的摊盘重摆）与 `plates` 人工选板（`{源序号: [板号]}`，缺省全选）
- 验证：Bambu CLI 无头切片闭环——真实双源（3 板 + 3 板）合并产物 6 块板全部独立切片成功，逐板 gcode 用料槽号与源的料槽映射逐一吻合（含一板 4 色的多色板），13 槽全集调色板全程生效

## [2.6.3] - 2026-09-17

### 修复
- **合并导出颜色仍丢失（"3mf 文件配置无效，仅加载几何数据"）**：Bambu 只在模型文档 `<metadata name="Application">` 为 BambuStudio 标识时才按自家工程加载——否则丢弃 `project_settings.config` 的料槽表，所有对象回落默认单色。现产物 Application 写为 `BambuStudio-<版本>`（出处改由 Description 元数据保留）
- **project_settings 以第一个带配置源的完整 JSON 为基底**（约 570 键，含 `filament_settings_id` 等预设映射），并把**所有按料槽对齐的数组逐块等比扩缩**到合并后的料槽数——这些数组有 8/16/32/64 多种长度档（含冲刷矩阵 N×N），只对齐直排数组会因长度自相矛盾被 Bambu 整包判无效；随后覆盖 `filament_colour`/`filament_type` 为压缩后的合并调色板
- **model_settings.config 补齐 `<part>` 子树**（名称、列主序 4x4 矩阵、mesh_stat 面数），与 Bambu 原生结构一致；摆盘起点跳过 `bed_exclude_area`（如 P1S 左前角 18×28mm 切料器区），避免对象贴排除区告警
- 验证：Bambu CLI 无头回写闭环——合并产物被 Bambu 完整加载后重新导出，7 色料槽表与 12 个对象的料槽映射逐一保留

## [2.6.2] - 2026-09-16

### 修复
- **合并导出颜色丢失**：Bambu 工程的颜色不在模型 XML 里，而在 Metadata 配置——对象料槽在 `model_settings.config`、调色板在 `project_settings.config`（JSON），此前合并产物只搬几何导致全部单色。现按源读取这两份配置，wrapper 改为每个 build item 一个（对应 Bambu 的「对象」粒度），并在产物中输出 sidecar 配置：对象 id 用重编号后的新 id，料槽压缩到「实际用到的」并按源顺延重排；同一网格被多板共享而料槽不同（多板工程常态）时按料槽上下文分别复制上色。同时照常合成 3MF 标准 basematerials 并给三角形写 pid/p1（Bambu 的加载器忽略之，供遵循规范的工具使用）。已知边界：对象内焊接的多部件配色（源导出时已焊成整体网格）无法恢复，按对象级料槽上色
- **合并导出排版混乱/超床面**：原布局对每个源的**整体包围盒**做网格平移，源内部零件摊得很开时产物超出 256mm 床面（Bambu 报「超出盘边界」）。改为逐 build item 货架式摆盘——按各 item 世界包围盒排进床面（256mm 留 3mm 边距、间距 5mm、放不下自动换行），底面统一贴床

## [2.6.1] - 2026-09-16

### 修复
- **合并导出的 3mf 在 Bambu Studio 打不开（「加载模型文件失败/此文件不包含任何几何数据」）**：重编号表的键是（内部文档名, 对象 id），而所有源的主文档都叫 `3D/3dmodel.model` 且对象 id 常撞号——同一模型的两个版本合并时后一个源会覆盖前一个源的编号，产出重复对象 id、编号断档、装配树串源，切片器解析即失败——键加入源序号按源隔离（新增回归测试：同名同号双源合并且几何不串）
- 合并产物顺带剥离 production 扩展属性（`p:UUID` 等）：拍平后的新包不声明 production 语义，而同一模型合并两份时 UUID 必然重复；材质引用（pid/materialid）重映射增加主文档回退（Bambu 把 basematerials 放在主文档、网格对象在部件文档，原先只查对象所在文档会漏改写）

## [2.6.0] - 2026-09-15

### 新增
- **合并导出**：模型库「文件视图」新增多选模式——勾选 2~20 个模型一键拼盘合并为 1 个新 3mf：各源网格拍平进单文档（对象 id 统一重编号、跨部件引用改写），按包围盒网格自动并排摆放（只动 X/Z 平移、底面对齐 Y=0，互不重叠），新文件写入 `library_root/exports/` 并按上传同款管线入库（「待整理」新记录，可正常归档/重命名/上传缩略图），同时自动创建分组（新记录为主、源文件为组件、封面=新记录）
- **`POST /api/merge-export`**：合并导出端点（2~20 个 ids，任一源缺失/损坏/单位不一致整体拒绝，不产生半成品）；新增 `merge_3mf.py` 纯标准库合并模块（zipfile + ElementTree）
- **合并缩略图选导**：确认弹窗中可从各源文件的缩略图（缺省退回首张摆盘图）中点选一张作为新记录的缩略图，亦可选「不导入」；图片为复制而非移动，源图不动
- 合并全程**只读源文件**，原文件字节与索引记录一律不动；合并产物不含切片配置，打印前需用 Bambu Studio 重新切片；含贴图材质的模型暂不支持

## [2.5.1] - 2026-09-14

### 修复
- **重复副本误删防护**：对话结果面板「删除重复副本」在退回整理刷新后可能把同内容组中最早的副本也纳入删除范围（与确认框「每组中最早的副本保留」的承诺不符）——前端四处判定（状态徽章 / 操作条 / 清理过滤 / 刷新合并）统一为「仅非最早副本」口径；`/api/delete` 服务端同步加固：同内容多份时拒绝删除最早副本，先删其他副本、删到最后一份自然放行
- **Esc 分层关闭**：详情抽屉、图片查看器、确认弹窗此前各自监听 Esc，一次按键把整叠层全部关掉（如在抽屉上按 Esc 取消确认框，抽屉也被连带关闭）——现统一为栈式处理，一次只关最顶层（AI 计划预览 > 弹窗 > 查看器 > 抽屉）；图片查看器左右方向键行为不变
- **分组关系图「变体」连线不可见**：连线与图例引用的 `--purple` 变量在四个主题块中均未定义，stroke 实际计算为 none——补定义主题渐变第二色（浅色 #7b4ff0 / 深色 #9a6ff5）
- **查看器 ◀/▶ 翻页按钮无样式**：按钮没有 class，渲染为浏览器原生浅灰立体按钮嵌在深色头部——补齐与缩放按钮一致的深色圆角样式
- **内联 onclick 拼接动态串收尾**：`removeTag` / `applyCategory` 此前把标签名、AI 分类/归档名直接拼进 onclick 的 JS 字符串，值含英文单引号时按钮点击直接语法错误失效——改为 data 属性承载数据、onclick 只传 id 与元素
- **输入健壮性**：全部 7 处内联 Enter 提交（对话 / 抽屉表单 / 标签 / 分组重命名 / AI 纠正输入）增加 `isComposing` 检查，中文输入法按 Enter 上屏拼音不再误触发；上传框 change 后清空 value，同一份 3mf 可连续重选；抽屉底部「归档」按钮与卡片口径对齐（非最早副本禁用并提示原因），`applyOne` 失败 toast 透出服务端具体错误
- **列表与交互一致性（审计 P2）**：模型库翻页越界（最后一页文件被归档/删除）自动回退有效页而非显示假「未找到匹配模型」；`loadPending`/`search` 请求失败给出明确空态，不再卡骨架屏；结果面板勾选状态跨重渲染保留、「全选」文案随状态切换、init 后底栏提示不再空白；AI 分组建议点「忽略」后保留其余勾选、置信点数与整理计划口径统一；移出成员致分组清空时同步解散视图并提示，不再残留幽灵组卡片；对话页「加入分组…」先拉取分组数据，修复误报「库中还没有分组」；附件名点击可真实下载（原为永远无动作的死链）；危险确认框聚焦取消键，回车不再直接执行
- **微瑕收尾（审计边角）**：对话回复流式返回期间禁止再次发送与清空对话（发送按钮同步禁用），不再出现两条回复交错；卡片「⋯」菜单点「关联附件」后立即收起菜单；删除非当前抽屉文件时不再连带关闭抽屉；组内无未打印文件时点「全部标记已打印」给出准确提示而非误报成功，成功提示附带数量

## [2.5.0] - 2026-09-13

### 新增
- **对话结果面板智能交互**：结果面板按文件状态给出匹配的操作——行内新增状态徽章（待整理/已归档/重复副本），计数显示状态构成；操作条随勾选动态生成：勾选含待整理才出现「归档 N 个」（混合选择自动只归待整理并提示），全部已归档时归档按钮不再出现；新增「建成分组…」（≥2 个勾选，预填建组弹窗）、「加入分组…」（批量入组）、「删除重复副本」与「退回整理」（均有红色二次确认）。服务端对话返回的文件行补 `status` 与 `is_duplicate`（全库 SHA256 判定非最早副本）
- **AI 提议扩展到打标签与分组**：META action 白名单从仅 `archive` 扩展为 `archive/tag/group`，AI 建议打标签或"把这几个建成分组"时渲染统一确认条，确认后才执行（`/api/groups` 的 `member.add` 支持批量 `file_ids`）
- **文件分组全手动管理**：此前建立分组只有「✦ AI 找关联」一个入口，现支持完全人工操作——
  - 模型库工具栏与分组空状态新增「＋ 新建分组」：搜索勾选文件（可多选）、指定唯一主文件后创建
  - 分组卡「展开成员」升级为管理面板：逐成员下拉设置组内角色（★ 主文件 / 组件 / 变体 / 配件 / 重复 / 其他）、打印勾选、移出分组；面板级「＋ 添加文件」（搜索勾选追加成员）、「✎ 重命名」、「解散分组」（二次确认，只删分组关系）
  - 详情抽屉「所属分组」：组内角色可就地修改，下拉选「★ 主文件」即转移主文件；无组文件提供「加入分组… / 新建分组…」入口
- **组内角色新增「其他」（other）**：用于相关但难以归入组件/变体/重复/配件的文件；AI 判型提示词同步并明确「主文件每组仅一个、其余角色不限数量」；关系小图、图例与分组卡统计行支持「其他」

### 变更
- 界面统一「分组」称谓：模型库「作品」视图更名「分组」，「✦ AI 找关联」更名「✦ AI 建议分组」（AI 建议弹窗同步更名「分组建议」），相关提示文案与文档同步；弹窗内的「关联信号」字样保留，用于解释文件为何相关

### 行为
- 全部分组操作只写 `asset_groups` / `group_members` 关系层，不移动、不删除、不修改文件本体的任何属性；解散分组仅删除分组关系
- 组操作后分组卡片原位刷新，展开的管理面板保持打开；主文件/角色变更即时同步当前打开的文件抽屉
- 接口层防御：非 `GROUP_ROLES` 内的角色值一律回退 `component`（旧数据不受影响）

## [2.4.0] - 2026-09-12

AI 交互增强（分支 `feat/ai-interaction`，五个批次，交互设计稿见 `design/ai-interaction-mockups.html`）。

### 新增
- **批量 AI 整理**：待整理页一键「✦ AI 整理 (N)」——SSE 逐个推送分类进度（进度条/当前文件/规则跳过计数/可取消），完成后进入「整理计划」预览表：置信度分级（低置信默认不勾）、AI 新分类聚合一次确认、逐行改分类/归档名/路径、失败行补充一句话描述即时重判；「应用计划」批量归档。新增 `/api/llm-batch-classify`（SSE）与 `/api/apply-plan`；`llm_classify` 输出 `confidence` 自评
- **AI 分类对话式纠正**：建议盒支持「不对？告诉它为什么」——纠正输入 + 快捷 chips，上下文逐轮累积（最多 5 轮），轮次徽章 + 置信度展示；`/api/llm-classify` 接受 `history` 多轮参数
- **对话助手可操作化**：对话页改左右分栏（左对话/右结果面板）；回复内嵌文件卡片（点击开详情抽屉）；AI 提议归档时渲染确认执行条，确认后才执行（Propose-Confirm，action 白名单仅 archive）；结果面板聚合对话中提到的文件，可勾选批量归档/打标签
- **流式对话**：新增 `/api/chat/stream`（SSE 打字机输出）；提示词改为「正文 + META 行」协议，元数据截留不泄漏到可见回复；`llm_client` 新增零依赖流式解析 `chat_stream`
- **教学闭环**：抽屉手动改分类保存后 toast 提供「✦ 以后同类文件都这样分」→ 规则确认弹窗 → 写入学习规则；设置页新增「已学习的分类规则」管理（命中次数透明、可删除）；`categorize()` 学习规则最高优先匹配，摄入时自动计数命中
- **AI 状态体系**：对话失败气泡就地重试；AI 分类失败带重试；设置页显示「上次 AI 调用耗时」

## [2.3.0] - 2026-09-11

### 界面体验（P2）
- **上传进度条**：上传改用 XHR 进度回调，上传区显示渐变进度条与百分比；100% 后进入「解析入库中…」状态，上传期间拖放区禁用防重复提交
- **查看器升级**：深色画布（黑白线稿与彩色照片都更耐看）；新增滚轮/双指捏合缩放（以光标为中心，1×–8×）、双击切换放大、放大后拖拽平移；工具栏新增 −/百分比/+/适配窗口 控件
- **对话体验**：欢迎示例改为可点击 chips（点击即发送）；「思考中…」升级为三点跳动动画；对话区高度自适应视口；LLM 未配置时报错附带「去设置」直达按钮
- **暗色模式**：全套 token 深色主题，默认跟随系统偏好；导航底部新增主题切换按钮（跟随系统→深色→浅色三态循环，localStorage 持久化）；清理残留硬编码颜色统一改 token

### 界面体系化（P1）
- **内联 SVG 图标**：新增 9 枚线性图标（相机/垃圾桶/复制/打印机/文件夹/AI 星标/眼睛/编辑/返回），替换全部 emoji 图标，跨平台渲染一致、风格统一
- **统一确认对话框**：删除文件/退回整理/删除缩略图/初始化最后确认 4 处原生 `confirm()` 全部替换为与全站一致的模态确认（红色主按钮 + 后果加粗说明），ESC/点击遮罩视为取消
- **详情抽屉**：卡片「展开信息」改为右侧滑出详情抽屉——大图预览、归档名/分类/归档路径编辑（分类带全量建议 datalist + AI 入口）、文件信息（含上传/归档时间）、标签与附件管理、按状态区分的操作按钮，卡片回归紧凑头部布局
- **导航徽章**：左侧「待整理」导航项显示橙色圆点徽章（有待整理文件时出现），复用既有样式
- 清理死代码：归档路径浮层、内联编辑一族函数与 `.target-pop`/`.card-info`/`.path` 等死样式移除

### 界面美化（P0 快赢）
- **字阶与字重**：页标题/面板标题/卡片标题/大数字提升到 600 字重并微调字号，页面信息层级更有锚点；统计数字启用等宽数字排版
- **卡片展开态对齐**：网格 `align-items:start`，修复一张卡展开时同行其余卡片被拉伸出现大片空白的问题
- **徽章降噪**：卡片元信息徽章从 4 个减到 2 个（分类 + 摆盘×N）；体积与板数移入缩略图左下角半透明小角标
- **帮助样式去警示化**：设置页接口说明由黄色警示框改为中性灰说明框，黄色只留给真正需要注意的事项
- **品牌 favicon**：新增蓝紫渐变 3D 立方体 SVG 图标（data URI，零额外请求）

### 工程化
- **版本号单一来源**：新增 `version.py`，`pyproject.toml` 通过 setuptools dynamic attr 读取，服务启动横幅同步显示，消除 pyproject 与 CHANGELOG 版本漂移
- **CI**：新增 GitHub Actions（`.github/workflows/ci.yml`），Python 3.10/3.13 双版本矩阵，执行 `ruff check` + `pytest`
- **ruff 静态检查**：加入 dev 依赖并全库清零（清理未用导入/未用变量；单行分号等既有风格加入忽略清单，不做整库改写）
- **归档路径校验加固**：`set-target` 按段校验，拒绝 `..`/`.`/盘符冒号路径（修复 Windows 盘符绝对路径可逃逸库根），路径分隔符归一化为 `/`
- **测试基建**：`client` fixture 上移 `conftest.py`；新增 `set-target` 校验回归测试

### 性能与体验
- **`/api/files` 服务端分页**：新增 `limit`/`offset` 参数与 `total` 字段；重复（SHA256）标记改为在**全量过滤集**上计算后再切页，同组重复跨页也标记正确；模型库页改为服务端分页，大库不再全量拉取
- **消除附件 N+1 请求**：`/api/files` 每行内嵌 `attachments` 数组（单条 IN 查询聚合），前端渲染卡片直接使用，50 张卡片从 51 个请求降为 1 个
- **缓存策略**：缩略图（文件名含随机指纹）`immutable` 长缓存、附件按天缓存、静态资源短缓存，页面与 API 保持 `no-store`
- **大文件上传落盘**：请求体超过 64MB 时 spool 到临时文件并以 mmap 解析，文件内容以只读流直写磁盘，不再整包驻留内存
- **LLM 上下文预筛**：对话与语义检索先按消息关键词 LIKE 预筛（无命中回退最近文件），不再把全库清单塞进提示词

### 架构
- **模块拆分**：`server.py`（组合根 + HTTP 路由表）、`classify.py`（分类/别名/目录规划纯逻辑）、`db.py`（存储层：schema/迁移/路径工具，显式传参不持全局）；路由由 if 链改为 `GET_ROUTES`/`POST_ROUTES` 表驱动
- **分类规则数据化**：IP 关键词、目录映射、噪音词等外置到 `rules.json`，可直接编辑扩充，改完重启生效
- **schema 版本化迁移**：`PRAGMA user_version` 逐级迁移（v1 基础表、v2 `files.rel_path`/`attachments.rel_path`）；`files` 表新增相对库根的 `rel_path`，切换模型根目录后自动按 rel_path 重定位绝对路径，换盘/迁移目录不再失效
- **日志落地**：`logs/mfmanager.log` 轮转日志 + 控制台输出，替换关键路径的静默 `except: pass`，未捕获异常记录堆栈

### 安全
- **修复 `/static/`、`/thumbs/` 路径穿越**：`GET /static/../library.db` 等可越出静态目录读到源码与数据库，现对路径做 realpath 前缀校验，越界一律 404
- **归档名（alias）清洗**：手动设置与 LLM 返回的归档名在入库与归档落盘两侧统一清洗（去非法字符、压连字符、去首尾点/横杠），杜绝 `../` 注入把文件写出模型根目录
- **全局异常兜底**：`do_GET`/`do_POST` 统一包裹，未捕获异常返回 500 JSON 并记录堆栈，不再无痕断连；非法 JSON 请求体返回 400

### 修复
- **自定义分类打通**：AI 确认的新分类此前写入后既不参与规则分类、也不出现在手动重选下拉里（死数据）。现 `categorize()` 按分类名最具体末段优先匹配，`/api/categories` 返回 `custom` 字段，前端重选下拉可见可选
- **缩略图替换泄漏**：更换缩略图时同步删除旧图片文件，`thumbs/` 不再累积孤儿文件；文件名加随机后缀，同秒替换不再重名
- **上传 SHA-256 重复计算**：上传时算好的 hash 直接传入解析入库，大文件上传 I/O 减半
- **multipart 二进制截断**：解析只剥 boundary 前的一个尾部 `\r\n`，不再误截内部含 `\r\n` 的二进制内容（如 PNG 文件头）

## [2.2.2] - 2026-08-13

### 新增
- **AI 自动分类支持用户分类提示**：点击卡片分类旁的 🤖 按钮，先弹出「分类提示」输入框，含「✓ 继续」「✕ 取消」两个按钮。填写提示后模型**优先参考用户提示**进行分类（若提示已明确指定目标分类则优先采纳）；留空则沿用原有自动分类逻辑。同时增强分类提示词：遇到不认识的专有名词/角色/IP/作品名时，模型可通过**网络搜索**确认其类别后再判断。`POST /api/llm-classify` 新增可选 `hint` 字段（前后端均对空白输入做归一化处理，空提示与旧行为一致）
- **缩略图手动上传入口**：卡片缩略图角落新增相机按钮（hover 显示，📷），点击选择本地图片上传/更换缩略图（png/jpg/jpeg/webp/gif），上传后待整理与模型库即时刷新；无图时提示「上传缩略图」、有图时提示「更换缩略图」
- **缩略图 HEIC 自动转换**：从 Apple 照片图库选图上传（默认 HEIC）不再失败——后端按**文件内容**识别图片格式（不依赖扩展名），HEIC/HEIF/BMP/TIFF 等自动用系统工具（macOS `sips` / Linux ImageMagick）转成 JPEG 后落盘，转换成功提示「已上传并自动转换」；无法识别的内容直接拒绝并给出明确提示（零第三方依赖）
- **修复 multipart 解析导致从 Apple 图库选图上传报「need image」**：`_read_multipart` 原仅识别带双引号的 `filename="x"`，而 macOS 从照片图库选图时浏览器发出的 `Content-Disposition` 常为无引号 / `filename*=` / 无 filename 格式，导致文件 part 被误当普通字段、files 为空。已兼容全部格式并加 `name=file` 兜底，缩略图/附件/3MF 上传均受益
- **缩略图可在预览中删除**：点击缩略图打开预览查看器后，仅当当前展示的是「模型预览」（缩略图）时，头部显示 🗑 删除按钮；点击确认后清空缩略图并删除图片文件，卡片回到「未上传」占位。新增 `POST /api/delete-thumb` 接口
- **用 Bambu Studio 打开 3MF**：卡片归档路径行新增 🖨 入口（待整理/已归档均可用），点击调用系统 `open -a BambuStudio` 打开该卡片对应的 3MF 文件；BambuStudio 不可用时回退系统默认打开。新增 `POST /api/open-in-bambu` 接口
- **Safari 照片图库选图兼容**：Safari 从照片图库选图时请求体为空（浏览器侧问题），前端增加 `file.size` 检测并提示改用 Chrome 或先导出图片；后端 multipart body 读取支持 chunked 编码增强健壮性
- **AI 分类输出归档名建议并同步采用**：LLM 分类提示词增加 `alias` 输出（基于标题/内容提炼的简短归档名，去除版本号/尺寸/打印参数等噪音词，≤26字），前端展示归档名建议；点击「采用」时分类、归档名、归档路径（基于分类重算 `target_dir`）三者同步生效。`/api/recategorize` 与 `/api/confirm-new-category` 接收 `alias`，模型给的优先、为空则 `make_alias` 兜底

## [2.2.1] - 2026-08-09

### 修复
- **「确认全部归档」误归档重复文件**：此前会把每组重复文件中的最早一份也一并归档；现改为只归档非重复文件，整组重复文件全部跳过（前端过滤 `is_duplicate`，并在提示中展示跳过数量）
- **修复待整理「删除文件」失效**：卡片菜单中删除按钮的 `onclick` 使用了模板字符串插值 `${f.id}`，但因该插值写在内层单引号字符串内未被求值，原样渲染成字面量 `delFile(${f.id})`，点击触发 `missing ) after argument list` 语法错误导致删除完全不执行（不弹确认框、不发请求）。已改为字符串拼接 `delFile('+f.id+')`，并确认真实浏览器下删除恢复可用
- **「重复」标记只显示在不可归档的副本上**：此前每组重复文件的两份都显示红色「重复」；现改为仅不可归档的后进副本显示「重复」标记，可归档的最早副本不再显示，避免误导
- **归档路径可编辑（支持子目录）**：待整理状态下归档路径可点击编辑，支持输入子目录路径作为归档位置；已归档状态归档路径只读、「在文件管理器中打开」图标仅已归档显示。新增 `/api/set-target` 接口（仅待整理可改，含路径安全校验：拒绝绝对路径与 `..` 穿越；空路径回退到分类默认路径）
- **添加标签去重**：添加标签时若已存在则不再重复添加（视为已关联，提示「标签已关联」）；后端 `/api/tags` 也做去重兜底，保证不产生重复标签
- **标签增删改为局部更新**：添加/删除标签后仅更新该卡片的标签列表（`.taglist`），不再整体重渲染列表，保留正在编辑的输入框、展开的信息区与滚动位置，体验更好
- **归档路径选择器**：编辑归档路径时弹出浮层，从已有目录列表中选择基础路径（可点击项，靠近焦点），再在其下输入子路径；子路径不存在时归档自动创建（`os.makedirs`）。新增 `GET /api/dirs` 列出已存在目录（排除 `00_待整理` 与隐藏项）
- **初始化模型根目录后重置自增 id**：清空索引的同时清空 `sqlite_sequence`，后续上传文件 id 从 1 重新编号，完全恢复新装状态

## [2.2.0] - 2026-08-09

### 新增
- **设置页「模型根目录」改名**：原「收藏根目录」更名为「模型根目录」；默认目录名由中文 `3D模型库` 改为英文 `3mf_data`（已配置用户不受影响，仍以用户配置优先）
- **初始化模型根目录**：设置页新增危险操作按钮，点击后二次确认并需完整输入模型根目录路径做比对，确认后才清空该目录全部数据、重建 `00_待整理` 并重置索引（防误删）
- **关于菜单**：设置页新增「关于」说明，以 Markdown 加载并渲染项目功能、使用规则、最近更新与版本信息，面向普通用户、不偏技术

## [2.1.0] - 2026-08-09

### 新增
- **摆盘缩略图提取**：解析时自动提取 3MF 内嵌的模型预览图与各打印板摆盘图（`Auxiliaries/.thumbnails/*` + `Metadata/plate_N.png`），卡片显示，点击放大、键盘/按钮切换多板视图
- `parse_3mf.extract_previews()`：解压内嵌图，零 3D 渲染成本
- `plates` 计数修正：不再把 `plate_N.png` 与 `plate_N.json` 重复计入
- 数据库新增 `plate_imgs` 字段（自动迁移）

## [2.0.0] - 2026-08-09

### 新增
- **LLM 模型能力集成**：OpenAI 兼容接口（本地 Ollama/vLLM、DeepSeek、通义等）
- **模型辅助分类**：信息提取后用 LLM 复核/分类；现有分类不满足可**新增分类建议**，用户确认后执行
- **语义搜索 + 多轮对话智能体**：自然语言检索模型库
- **Dashboard 仪表盘**：资产总览、分类分布、Hash 重复提示
- **附件关联**：为 3MF 关联非 3MF 附件（说明/STL/PDF）
- **Hash 重复检测**：上传时 SHA-256 重复自动提示并标红
- **设置界面**：模型能力（地址/key/模型名）+ 本地路径可视化配置
- **手动改分类**：对待整理文件可重选分类

### 变更
- 升级为开源项目规格：MIT 协议、`uv` 包管理、`pytest` 测试套件、`git` 管理
- 解析/分类/别名/子分类模块由外部软链改为**项目内自包含文件**

## [1.0.0] - 2026-08-09

### 新增
- 上传 3MF → 解析 → 预分类 → 别名 → SHA-256 → 设计ID 展示
- 确认归档：移动（分类罗盘）+ 重命名，更新后台索引
- Tag 标注、全文/分类/tag/hash/设计ID 查询（显示本地磁盘路径）
- 缩略图上传（可选）
