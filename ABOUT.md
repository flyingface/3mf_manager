# 关于 3MF Manager

**版本：2.6.0**

3MF Manager 是一款帮你整理本地 3D 打印模型文件（3MF 格式）的小工具。它把散落在电脑里的模型文件集中管理起来，自动识别信息、分类归档、去重检索，让你随时都能找到想要的模型。

---

## 一、它能做什么

- **集中存放**：你上传的模型文件会统一放进「模型根目录」，不再东一个西一个。
- **自动识别**：上传时自动读取模型标题、作者、设计编号等信息。
- **智能分类**：根据模型类型自动归类（如 IP 联名、功能件等），也能借助 AI 辅助判断。
- **一键归档**：确认后按分类自动移动到对应文件夹，并按规则重命名。
- **快速检索**：支持按名称、分类、标签、编号搜索，还能用大白话跟它对话查找。
- **关联附件**：可为模型补充打印说明、STL、PDF 等参考资料。
- **去重提示**：如果上传了内容完全相同（SHA256 一致）的文件，会自动标红提醒。

---

## 二、基本操作

1. **上传**：在「待整理」页把 3MF 文件拖进来即可，系统会自动解析。
2. **归档**：在「待整理」页确认分类与名称后点「归档」，文件进入「模型库」对应分类文件夹。归档前可点「归档路径」编辑：从已有的目录列表中选择一个作为基础，再在其下输入子路径作为归档位置（支持多级子目录，归档时自动创建）。
3. **检索与管理**：在「模型库」按条件筛选；点卡片右上角 ⋯ 可关联附件、退回整理等。
4. **退回整理**：已归档的文件若想重新调整，可「退回整理」——文件与索引状态一起退回「待整理」，方便再次编辑后归档。
5. **删除**：仅在「待整理」状态可删除；删除时源文件进入回收站（可恢复），索引一并清除。

---

## 三、使用规则

- **重复文件**：内容完全相同的多个副本。其中**最早的一份**可正常归档、不带「重复」标记；其余副本会带红色「重复」标记且不可归档。「确认全部归档」时**只归档非重复文件**，会跳过整组重复文件；单个文件手动归档时，仅允许归档该组**最早的一份**，避免重复堆积。
- **归档权限**：能否归档、退回，只与文件当前状态有关，与你在哪个页面查看无关。
- **分类目录**：归档后的文件按分类放入子目录；「00_待整理」专门存放尚未归档的文件。
- **模型根目录**：所有模型文件与子文件夹都在该目录里，可在「设置」中修改；一旦修改，新上传会落到新目录。

---

## 四、最近更新

- **2.6.0**：合并导出——模型库新增「多选」模式，勾选 2~20 个模型一键合并为 1 个新 3mf（自动并排摆放、互不重叠），新记录进入「待整理」并自动建组（新文件为主、原文件为组件），还可从源文件缩略图中点选一张作为新记录缩略图；全程只读原文件。合并产物不含切片配置，打印前请用 Bambu Studio 重新切片。
- **2.5.0**：文件分组支持全手动管理——手动「＋ 新建分组」（搜索勾选文件、指定唯一主文件）、展开成员面板逐个设置组内角色（★主文件/组件/变体/配件/重复/其他）、添加/移出成员、重命名、解散分组；文件抽屉可就地加入分组或改角色。对话结果面板按文件状态给出匹配操作（归档/建组/入组/打标签/清理重复副本/退回整理），AI 提议打标签与建组同样确认后执行。分组操作只记录关系，不移动不修改文件本体。
- **2.4.0**：AI 交互增强——待整理页一键「✦ AI 整理」批量分类（进度实时可见，生成整理计划后统一确认应用）；分类建议可对话式纠正；对话助手分栏布局、流式输出，回复中的文件卡片可直接操作（归档确认、批量归档/打标签）；手动改分类可一键学习为规则，下次同类文件自动命中。
- **2.3.0**：安全加固（修复静态目录路径穿越、归档名与归档路径注入防护）、AI 新建的自定义分类可直接在重选分类中使用、模型库分页与缓存提速（大库更流畅）、大文件上传更省内存、切换模型根目录后索引自动跟随、分类规则可编辑（`rules.json`）。
- **2.2.1**：修复「确认全部归档」误归档重复文件——现在只归档非重复文件，整组重复文件会被跳过；修复待整理卡片「删除文件」无效的问题；「重复」标记只显示在不可归档副本上；待整理下归档路径可编辑（从已有目录中选择并可输入子路径）、已归档只读并显示「打开」图标；添加标签去重、增删标签局部更新不整页刷新。
- **2.1.0**：自动提取模型摆盘缩略图，卡片可直接查看多块打印板的预览图。
- **2.0.0**：接入 AI 模型能力（智能分类、语义搜索、对话助手）；新增仪表盘、附件关联、去重检测与设置界面。
- **1.0.0**：基础的上传、解析、归档、检索与缩略图功能。

---

## 五、重要提醒

- 「设置」中的 **初始化模型根目录** 会**永久清空**该目录下的全部模型文件、子文件夹与索引记录，且**不可恢复**。操作前请务必确认路径无误，并已做好备份。
- 本工具的所有数据都保存在你本机，不会上传任何云端。
- 本工具为**非官方工具**，与拓竹科技（Bambu Lab）及其平台 MakerWorld 无任何隶属、合作或赞助关系，亦未经其认证或认可；Bambu Lab、Bambu Studio、MakerWorld 等名称与标识为其权利人的商标，此处仅在描述兼容性与互操作时作叙述性引用。
- 本工具不包含、不分发任何 3MF/模型文件，你导入的文件版权归原作者所有，请遵守来源平台的服务条款与原作者许可协议。

---

*如需更多帮助，可在「设置」中查看模型根目录与模型配置。*
