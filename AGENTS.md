# AGENTS.md — AI 编码代理核心规则

本文件约束所有在本仓库工作的 AI 编码代理（ZCode / Claude Code / Codex 等）。以下规则为**核心规则**，优先级最高，任何任务都不得违背。

## 🔒 核心规则：用户数据不可侵犯

1. **严禁修改、删除历史导入的数据。**
   - `library.db` 中的既有记录、`library_root`（模型根目录）下的用户文件、`thumbs/`、`attachments/`、`.trash/` 中的既有内容，全部视为**用户真实资产**。
   - 无论任务看起来多么合理（重构、清理、去重、修复索引），都不得直接改动或删除这些数据。

2. **所有变更必须向前兼容。**
   - SQLite schema 只允许**增量迁移**（新增表/列、`PRAGMA user_version` 递增），禁止删列、改列类型、重命名旧列等破坏性操作。
   - 新代码必须兼容读取旧数据（旧记录缺新列时给默认值，不得假设字段存在）。
   - 文件系统布局只能新增目录/文件，不得移动、重命名或删除既有用户文件。

3. **破坏性变更必须提供迁移脚本，并得到用户明确确认后才能执行。**
   - 如果某个改动不可避免地要转换/重写/删除既有数据（例如批量重算索引、合并表、移动用户文件），必须：
     a. 单独编写幂等的迁移脚本（自动备份原数据 → 转换 → 可回滚）；
     b. 向用户说明影响范围（多少条记录/文件、是否可逆）；
     c. **获得用户的明确同意后**才能运行；未经确认不得执行。

4. **测试数据的边界。**
   - 代理为验证功能而创建的临时数据，必须在任务结束前清理（删除自己创建的记录/文件，或明确告知用户保留原因）。
   - 清理动作仅限**代理自己本轮创建的数据**，绝不触碰历史数据。

## 违反后果

以上任何一条被违背，都可能造成用户不可恢复的资产损失。宁可多问一句、多做备份，也不要"顺手清理"。

## 其他工程约定

- 测试用 `uv run python -m pytest tests/`（本机 `uv run pytest` spawn 会失败）。
- 前端为单文件 `static/index.html`：批量改动后必须做 onclick 引用 vs 函数定义比对 + `node --check`。
- 提交信息遵循 Conventional Commits（中文描述）。
